# Youma SDBS Fresh Checkpoint Slot

This directory is intentionally empty for now.

Expected trained checkpoint:

```text
best_world_model.pt
```

Optional metadata:

```text
summary.json
manifest.txt
```

The dashboard mode `Youma SDBS fresh accident/overtake` will refuse to launch until `best_world_model.pt` exists here.
